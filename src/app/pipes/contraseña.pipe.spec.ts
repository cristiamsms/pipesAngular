import { ContraseñaPipe } from './contraseña.pipe';

describe('ContraseñaPipe', () => {
  it('create an instance', () => {
    const pipe = new ContraseñaPipe();
    expect(pipe).toBeTruthy();
  });
});
